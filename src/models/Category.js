// JSDoc-only model definition — no runtime code.

/**
 * @typedef {object} Category
 * @property {string} id
 * @property {string} name
 * @property {string} [tagline]
 * @property {string} image
 */

export {}
