# Tupperware Store — React App

React + Vite + Tailwind CSS + React Router rebuild of the approved design in
`../` (the vanilla JS reference implementation, kept unchanged on `main`).

Project foundation only — see the architecture document discussed in-session
for the full folder structure, routing, and data-flow plan.
